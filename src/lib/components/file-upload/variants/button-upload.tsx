import React from 'react'
import { FileUploadProps } from '../file-upload.types'

export const ButtonUpload: React.FC<FileUploadProps> = (props) => {
    // Placeholder implementation - will be expanded in later tasks
    return <div>Button Upload Variant</div>
}

ButtonUpload.displayName = 'ButtonUpload'