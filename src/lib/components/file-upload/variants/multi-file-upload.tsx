import React from 'react'
import { FileUploadProps } from '../file-upload.types'

export const MultiFileUpload: React.FC<FileUploadProps> = (props) => {
    // Placeholder implementation - will be expanded in later tasks
    return <div>Multi File Upload Variant</div>
}

MultiFileUpload.displayName = 'MultiFileUpload'