import React from 'react'
import { FileUploadProps } from '../file-upload.types'

export const PreviewUpload: React.FC<FileUploadProps> = (props) => {
    // Placeholder implementation - will be expanded in later tasks
    return <div>Preview Upload Variant</div>
}

PreviewUpload.displayName = 'PreviewUpload'